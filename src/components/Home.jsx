import React, { useContext } from 'react';
import { ProductContext } from '../App';

const Home = () => {
    const pro = useContext(ProductContext);
    console.log(pro);
    return (
        <div>
            ;kjkdfsjkgjfg
        </div>
    );
};

export default Home;